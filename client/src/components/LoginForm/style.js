import styled from 'styled-components';

const StyledWrapper = styled.div`
  .login-form {
    max-width: 300px;
  }
  .login-form-forgot {
    float: right;
  }
  .login-form-button {
    width: 100%;
  }
`;

export default StyledWrapper;
